spring:
  jpa:
    hibernate:
      ddl-auto: create
    show_sql: true
    open-in-view: false
  main:
    banner-mode: "off"
  dao:
    exceptiontranslation:
      enabled: false

spring.jpa.properties.hibernate.format_sql: false

logging:
  level.root: error